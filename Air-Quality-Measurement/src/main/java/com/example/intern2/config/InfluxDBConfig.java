package com.example.intern2.config;


import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;


@Configuration
public class InfluxDBConfig {
/*
    @Bean
    public InfluxDB influxDB() {
        String url = "http://localhost:8086"; // InfluxDB URL
        String username = "Ceyda";    // InfluxDB kullanıcı adı
        String password = "12345678";    // InfluxDB şifresi
        String  database = "sensor_data_db";    // Veritabanı adı

            InfluxDB influxDB = InfluxDBFactory.connect(url, username, password);
            influxDB.setDatabase(database);
            return influxDB;


        }*/
}
